import { Router } from "express";

export default class AttendanceRoutes {
    /**
     * Classe para definição das rotas de Atendimento
     */
    public constructor() {}

    public async execute(){
        const attendanceRouter = Router();

        return attendanceRouter;
    }
}